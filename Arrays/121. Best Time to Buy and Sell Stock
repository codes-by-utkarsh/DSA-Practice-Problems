// Bruteforce approach is easy but in optimised approach - we are updating the buy_price jaise hi koi naya number usse bada aarha hai or at the same time check kr rhe hai profit.

class Solution {
    public int maxProfit(int[] prices) 
    {
        int profit=0;
        int buy_price=prices[0];
        for(int i=0;i<prices.length;i++)
        {
            if(prices[i]<buy_price)
            {
                buy_price=prices[i];
            }
            else
            {
                int current_profit=prices[i]-buy_price;
                profit=Math.max(current_profit,profit);
            }
        }
        return profit;
    }
}
